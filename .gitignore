# Flutter / Dart build artifacts
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/.packages
**/build/
**/.dart_tool/

# Node (scraper / screenshot tooling)
tools/node_modules/
tools/video/
tools/frames/
tools/dbg/

# Ad-hoc screenshot scratch dirs (anywhere)
caps/
caps2/
**/caps/

# Release build artifacts (kept locally / attached to releases, not committed)
*.aab
*.ipa

# Misc
.DS_Store
*.log
/tmp/
